#include <iostream>
#include <string>
using namespace std;

int t;
int ascll[1011];
// int input;
string str;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    cin >> t;
    int i = 1;
    while (getline(cin, str)) {
        
    }

    return 0;
}