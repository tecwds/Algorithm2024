#include <bits/stdc++.h>
using namespace std;

int dp[105][105];

int main() {
    string s1;
    string s2;
    
    while (cin >> s1 >> s2) {
        memset(dp, 0, sizeof dp);
        
        for (int i = 0; i < s1.size(); ++i) {
        }
    }
    
    return 0;
}